{
  "name": "tradelens-submission",
  "private": true,
  "version": "1.0.0",
  "description": "Repo-root tooling, separate from app/ (the Vite frontend). Currently just the ONE_PAGER.md -> TradeLens_Submission.pdf build.",
  "scripts": {
    "build:pdf": "node scripts/build_pdf.js"
  },
  "devDependencies": {
    "pdf-lib": "^1.17.1",
    "puppeteer": "^23.0.0"
  }
}
